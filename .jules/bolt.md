## 2024-05-24 - Memoizing Data Transformations\n**Learning:** Components consuming context may re-render frequently if the context changes or parent re-renders. Complex data transformations (like `sort` or `reduce` on arrays) inside render functions can be performance bottlenecks.\n**Action:** Use `useMemo` to wrap heavy calculations, preventing them from running on every render when dependencies haven't changed.
